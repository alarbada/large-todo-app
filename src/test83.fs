module Test83
module Test831 =
  type Actions =
    | AddItem of string
    | RemoveItem of int

  type Actions2 =
    | AddItem of string
    | RemoveItem of int

  type Actions3 =
    | AddItem of string
    | RemoveItem of int

  let add x y = x + y
  let mul x y = x * y

  let three = add 3 4 |> mul 3 |> add 2 |> add 3


module Test832 =
  type Actions =
    | AddItem of string
    | RemoveItem of int

  type Actions2 =
    | AddItem of string
    | RemoveItem of int

  type Actions3 =
    | AddItem of string
    | RemoveItem of int

  let add x y = x + y
  let mul x y = x * y

  let three = add 3 4 |> mul 3 |> add 2 |> add 3


module Test833 =
  type Actions =
    | AddItem of string
    | RemoveItem of int

  type Actions2 =
    | AddItem of string
    | RemoveItem of int

  type Actions3 =
    | AddItem of string
    | RemoveItem of int

  let add x y = x + y
  let mul x y = x * y

  let three = add 3 4 |> mul 3 |> add 2 |> add 3


module Test834 =
  type Actions =
    | AddItem of string
    | RemoveItem of int

  type Actions2 =
    | AddItem of string
    | RemoveItem of int

  type Actions3 =
    | AddItem of string
    | RemoveItem of int

  let add x y = x + y
  let mul x y = x * y

  let three = add 3 4 |> mul 3 |> add 2 |> add 3


module Test835 =
  type Actions =
    | AddItem of string
    | RemoveItem of int

  type Actions2 =
    | AddItem of string
    | RemoveItem of int

  type Actions3 =
    | AddItem of string
    | RemoveItem of int

  let add x y = x + y
  let mul x y = x * y

  let three = add 3 4 |> mul 3 |> add 2 |> add 3


module Test836 =
  type Actions =
    | AddItem of string
    | RemoveItem of int

  type Actions2 =
    | AddItem of string
    | RemoveItem of int

  type Actions3 =
    | AddItem of string
    | RemoveItem of int

  let add x y = x + y
  let mul x y = x * y

  let three = add 3 4 |> mul 3 |> add 2 |> add 3


module Test837 =
  type Actions =
    | AddItem of string
    | RemoveItem of int

  type Actions2 =
    | AddItem of string
    | RemoveItem of int

  type Actions3 =
    | AddItem of string
    | RemoveItem of int

  let add x y = x + y
  let mul x y = x * y

  let three = add 3 4 |> mul 3 |> add 2 |> add 3


module Test838 =
  type Actions =
    | AddItem of string
    | RemoveItem of int

  type Actions2 =
    | AddItem of string
    | RemoveItem of int

  type Actions3 =
    | AddItem of string
    | RemoveItem of int

  let add x y = x + y
  let mul x y = x * y

  let three = add 3 4 |> mul 3 |> add 2 |> add 3


module Test839 =
  type Actions =
    | AddItem of string
    | RemoveItem of int

  type Actions2 =
    | AddItem of string
    | RemoveItem of int

  type Actions3 =
    | AddItem of string
    | RemoveItem of int

  let add x y = x + y
  let mul x y = x * y

  let three = add 3 4 |> mul 3 |> add 2 |> add 3


module Test8310 =
  type Actions =
    | AddItem of string
    | RemoveItem of int

  type Actions2 =
    | AddItem of string
    | RemoveItem of int

  type Actions3 =
    | AddItem of string
    | RemoveItem of int

  let add x y = x + y
  let mul x y = x * y

  let three = add 3 4 |> mul 3 |> add 2 |> add 3


module Test8311 =
  type Actions =
    | AddItem of string
    | RemoveItem of int

  type Actions2 =
    | AddItem of string
    | RemoveItem of int

  type Actions3 =
    | AddItem of string
    | RemoveItem of int

  let add x y = x + y
  let mul x y = x * y

  let three = add 3 4 |> mul 3 |> add 2 |> add 3


module Test8312 =
  type Actions =
    | AddItem of string
    | RemoveItem of int

  type Actions2 =
    | AddItem of string
    | RemoveItem of int

  type Actions3 =
    | AddItem of string
    | RemoveItem of int

  let add x y = x + y
  let mul x y = x * y

  let three = add 3 4 |> mul 3 |> add 2 |> add 3


module Test8313 =
  type Actions =
    | AddItem of string
    | RemoveItem of int

  type Actions2 =
    | AddItem of string
    | RemoveItem of int

  type Actions3 =
    | AddItem of string
    | RemoveItem of int

  let add x y = x + y
  let mul x y = x * y

  let three = add 3 4 |> mul 3 |> add 2 |> add 3


module Test8314 =
  type Actions =
    | AddItem of string
    | RemoveItem of int

  type Actions2 =
    | AddItem of string
    | RemoveItem of int

  type Actions3 =
    | AddItem of string
    | RemoveItem of int

  let add x y = x + y
  let mul x y = x * y

  let three = add 3 4 |> mul 3 |> add 2 |> add 3


module Test8315 =
  type Actions =
    | AddItem of string
    | RemoveItem of int

  type Actions2 =
    | AddItem of string
    | RemoveItem of int

  type Actions3 =
    | AddItem of string
    | RemoveItem of int

  let add x y = x + y
  let mul x y = x * y

  let three = add 3 4 |> mul 3 |> add 2 |> add 3


module Test8316 =
  type Actions =
    | AddItem of string
    | RemoveItem of int

  type Actions2 =
    | AddItem of string
    | RemoveItem of int

  type Actions3 =
    | AddItem of string
    | RemoveItem of int

  let add x y = x + y
  let mul x y = x * y

  let three = add 3 4 |> mul 3 |> add 2 |> add 3


module Test8317 =
  type Actions =
    | AddItem of string
    | RemoveItem of int

  type Actions2 =
    | AddItem of string
    | RemoveItem of int

  type Actions3 =
    | AddItem of string
    | RemoveItem of int

  let add x y = x + y
  let mul x y = x * y

  let three = add 3 4 |> mul 3 |> add 2 |> add 3


module Test8318 =
  type Actions =
    | AddItem of string
    | RemoveItem of int

  type Actions2 =
    | AddItem of string
    | RemoveItem of int

  type Actions3 =
    | AddItem of string
    | RemoveItem of int

  let add x y = x + y
  let mul x y = x * y

  let three = add 3 4 |> mul 3 |> add 2 |> add 3


module Test8319 =
  type Actions =
    | AddItem of string
    | RemoveItem of int

  type Actions2 =
    | AddItem of string
    | RemoveItem of int

  type Actions3 =
    | AddItem of string
    | RemoveItem of int

  let add x y = x + y
  let mul x y = x * y

  let three = add 3 4 |> mul 3 |> add 2 |> add 3


module Test8320 =
  type Actions =
    | AddItem of string
    | RemoveItem of int

  type Actions2 =
    | AddItem of string
    | RemoveItem of int

  type Actions3 =
    | AddItem of string
    | RemoveItem of int

  let add x y = x + y
  let mul x y = x * y

  let three = add 3 4 |> mul 3 |> add 2 |> add 3


module Test8321 =
  type Actions =
    | AddItem of string
    | RemoveItem of int

  type Actions2 =
    | AddItem of string
    | RemoveItem of int

  type Actions3 =
    | AddItem of string
    | RemoveItem of int

  let add x y = x + y
  let mul x y = x * y

  let three = add 3 4 |> mul 3 |> add 2 |> add 3


module Test8322 =
  type Actions =
    | AddItem of string
    | RemoveItem of int

  type Actions2 =
    | AddItem of string
    | RemoveItem of int

  type Actions3 =
    | AddItem of string
    | RemoveItem of int

  let add x y = x + y
  let mul x y = x * y

  let three = add 3 4 |> mul 3 |> add 2 |> add 3


module Test8323 =
  type Actions =
    | AddItem of string
    | RemoveItem of int

  type Actions2 =
    | AddItem of string
    | RemoveItem of int

  type Actions3 =
    | AddItem of string
    | RemoveItem of int

  let add x y = x + y
  let mul x y = x * y

  let three = add 3 4 |> mul 3 |> add 2 |> add 3


module Test8324 =
  type Actions =
    | AddItem of string
    | RemoveItem of int

  type Actions2 =
    | AddItem of string
    | RemoveItem of int

  type Actions3 =
    | AddItem of string
    | RemoveItem of int

  let add x y = x + y
  let mul x y = x * y

  let three = add 3 4 |> mul 3 |> add 2 |> add 3


module Test8325 =
  type Actions =
    | AddItem of string
    | RemoveItem of int

  type Actions2 =
    | AddItem of string
    | RemoveItem of int

  type Actions3 =
    | AddItem of string
    | RemoveItem of int

  let add x y = x + y
  let mul x y = x * y

  let three = add 3 4 |> mul 3 |> add 2 |> add 3


module Test8326 =
  type Actions =
    | AddItem of string
    | RemoveItem of int

  type Actions2 =
    | AddItem of string
    | RemoveItem of int

  type Actions3 =
    | AddItem of string
    | RemoveItem of int

  let add x y = x + y
  let mul x y = x * y

  let three = add 3 4 |> mul 3 |> add 2 |> add 3


module Test8327 =
  type Actions =
    | AddItem of string
    | RemoveItem of int

  type Actions2 =
    | AddItem of string
    | RemoveItem of int

  type Actions3 =
    | AddItem of string
    | RemoveItem of int

  let add x y = x + y
  let mul x y = x * y

  let three = add 3 4 |> mul 3 |> add 2 |> add 3


module Test8328 =
  type Actions =
    | AddItem of string
    | RemoveItem of int

  type Actions2 =
    | AddItem of string
    | RemoveItem of int

  type Actions3 =
    | AddItem of string
    | RemoveItem of int

  let add x y = x + y
  let mul x y = x * y

  let three = add 3 4 |> mul 3 |> add 2 |> add 3


module Test8329 =
  type Actions =
    | AddItem of string
    | RemoveItem of int

  type Actions2 =
    | AddItem of string
    | RemoveItem of int

  type Actions3 =
    | AddItem of string
    | RemoveItem of int

  let add x y = x + y
  let mul x y = x * y

  let three = add 3 4 |> mul 3 |> add 2 |> add 3


module Test8330 =
  type Actions =
    | AddItem of string
    | RemoveItem of int

  type Actions2 =
    | AddItem of string
    | RemoveItem of int

  type Actions3 =
    | AddItem of string
    | RemoveItem of int

  let add x y = x + y
  let mul x y = x * y

  let three = add 3 4 |> mul 3 |> add 2 |> add 3


module Test8331 =
  type Actions =
    | AddItem of string
    | RemoveItem of int

  type Actions2 =
    | AddItem of string
    | RemoveItem of int

  type Actions3 =
    | AddItem of string
    | RemoveItem of int

  let add x y = x + y
  let mul x y = x * y

  let three = add 3 4 |> mul 3 |> add 2 |> add 3


module Test8332 =
  type Actions =
    | AddItem of string
    | RemoveItem of int

  type Actions2 =
    | AddItem of string
    | RemoveItem of int

  type Actions3 =
    | AddItem of string
    | RemoveItem of int

  let add x y = x + y
  let mul x y = x * y

  let three = add 3 4 |> mul 3 |> add 2 |> add 3


module Test8333 =
  type Actions =
    | AddItem of string
    | RemoveItem of int

  type Actions2 =
    | AddItem of string
    | RemoveItem of int

  type Actions3 =
    | AddItem of string
    | RemoveItem of int

  let add x y = x + y
  let mul x y = x * y

  let three = add 3 4 |> mul 3 |> add 2 |> add 3


module Test8334 =
  type Actions =
    | AddItem of string
    | RemoveItem of int

  type Actions2 =
    | AddItem of string
    | RemoveItem of int

  type Actions3 =
    | AddItem of string
    | RemoveItem of int

  let add x y = x + y
  let mul x y = x * y

  let three = add 3 4 |> mul 3 |> add 2 |> add 3


module Test8335 =
  type Actions =
    | AddItem of string
    | RemoveItem of int

  type Actions2 =
    | AddItem of string
    | RemoveItem of int

  type Actions3 =
    | AddItem of string
    | RemoveItem of int

  let add x y = x + y
  let mul x y = x * y

  let three = add 3 4 |> mul 3 |> add 2 |> add 3


module Test8336 =
  type Actions =
    | AddItem of string
    | RemoveItem of int

  type Actions2 =
    | AddItem of string
    | RemoveItem of int

  type Actions3 =
    | AddItem of string
    | RemoveItem of int

  let add x y = x + y
  let mul x y = x * y

  let three = add 3 4 |> mul 3 |> add 2 |> add 3


module Test8337 =
  type Actions =
    | AddItem of string
    | RemoveItem of int

  type Actions2 =
    | AddItem of string
    | RemoveItem of int

  type Actions3 =
    | AddItem of string
    | RemoveItem of int

  let add x y = x + y
  let mul x y = x * y

  let three = add 3 4 |> mul 3 |> add 2 |> add 3


module Test8338 =
  type Actions =
    | AddItem of string
    | RemoveItem of int

  type Actions2 =
    | AddItem of string
    | RemoveItem of int

  type Actions3 =
    | AddItem of string
    | RemoveItem of int

  let add x y = x + y
  let mul x y = x * y

  let three = add 3 4 |> mul 3 |> add 2 |> add 3


module Test8339 =
  type Actions =
    | AddItem of string
    | RemoveItem of int

  type Actions2 =
    | AddItem of string
    | RemoveItem of int

  type Actions3 =
    | AddItem of string
    | RemoveItem of int

  let add x y = x + y
  let mul x y = x * y

  let three = add 3 4 |> mul 3 |> add 2 |> add 3


module Test8340 =
  type Actions =
    | AddItem of string
    | RemoveItem of int

  type Actions2 =
    | AddItem of string
    | RemoveItem of int

  type Actions3 =
    | AddItem of string
    | RemoveItem of int

  let add x y = x + y
  let mul x y = x * y

  let three = add 3 4 |> mul 3 |> add 2 |> add 3


module Test8341 =
  type Actions =
    | AddItem of string
    | RemoveItem of int

  type Actions2 =
    | AddItem of string
    | RemoveItem of int

  type Actions3 =
    | AddItem of string
    | RemoveItem of int

  let add x y = x + y
  let mul x y = x * y

  let three = add 3 4 |> mul 3 |> add 2 |> add 3


module Test8342 =
  type Actions =
    | AddItem of string
    | RemoveItem of int

  type Actions2 =
    | AddItem of string
    | RemoveItem of int

  type Actions3 =
    | AddItem of string
    | RemoveItem of int

  let add x y = x + y
  let mul x y = x * y

  let three = add 3 4 |> mul 3 |> add 2 |> add 3


module Test8343 =
  type Actions =
    | AddItem of string
    | RemoveItem of int

  type Actions2 =
    | AddItem of string
    | RemoveItem of int

  type Actions3 =
    | AddItem of string
    | RemoveItem of int

  let add x y = x + y
  let mul x y = x * y

  let three = add 3 4 |> mul 3 |> add 2 |> add 3


module Test8344 =
  type Actions =
    | AddItem of string
    | RemoveItem of int

  type Actions2 =
    | AddItem of string
    | RemoveItem of int

  type Actions3 =
    | AddItem of string
    | RemoveItem of int

  let add x y = x + y
  let mul x y = x * y

  let three = add 3 4 |> mul 3 |> add 2 |> add 3


module Test8345 =
  type Actions =
    | AddItem of string
    | RemoveItem of int

  type Actions2 =
    | AddItem of string
    | RemoveItem of int

  type Actions3 =
    | AddItem of string
    | RemoveItem of int

  let add x y = x + y
  let mul x y = x * y

  let three = add 3 4 |> mul 3 |> add 2 |> add 3


module Test8346 =
  type Actions =
    | AddItem of string
    | RemoveItem of int

  type Actions2 =
    | AddItem of string
    | RemoveItem of int

  type Actions3 =
    | AddItem of string
    | RemoveItem of int

  let add x y = x + y
  let mul x y = x * y

  let three = add 3 4 |> mul 3 |> add 2 |> add 3


module Test8347 =
  type Actions =
    | AddItem of string
    | RemoveItem of int

  type Actions2 =
    | AddItem of string
    | RemoveItem of int

  type Actions3 =
    | AddItem of string
    | RemoveItem of int

  let add x y = x + y
  let mul x y = x * y

  let three = add 3 4 |> mul 3 |> add 2 |> add 3


module Test8348 =
  type Actions =
    | AddItem of string
    | RemoveItem of int

  type Actions2 =
    | AddItem of string
    | RemoveItem of int

  type Actions3 =
    | AddItem of string
    | RemoveItem of int

  let add x y = x + y
  let mul x y = x * y

  let three = add 3 4 |> mul 3 |> add 2 |> add 3


module Test8349 =
  type Actions =
    | AddItem of string
    | RemoveItem of int

  type Actions2 =
    | AddItem of string
    | RemoveItem of int

  type Actions3 =
    | AddItem of string
    | RemoveItem of int

  let add x y = x + y
  let mul x y = x * y

  let three = add 3 4 |> mul 3 |> add 2 |> add 3


module Test8350 =
  type Actions =
    | AddItem of string
    | RemoveItem of int

  type Actions2 =
    | AddItem of string
    | RemoveItem of int

  type Actions3 =
    | AddItem of string
    | RemoveItem of int

  let add x y = x + y
  let mul x y = x * y

  let three = add 3 4 |> mul 3 |> add 2 |> add 3

