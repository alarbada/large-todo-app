module Test73
module Test731 =
  type Actions =
    | AddItem of string
    | RemoveItem of int

  type Actions2 =
    | AddItem of string
    | RemoveItem of int

  type Actions3 =
    | AddItem of string
    | RemoveItem of int

  let add x y = x + y
  let mul x y = x * y

  let three = add 3 4 |> mul 3 |> add 2 |> add 3


module Test732 =
  type Actions =
    | AddItem of string
    | RemoveItem of int

  type Actions2 =
    | AddItem of string
    | RemoveItem of int

  type Actions3 =
    | AddItem of string
    | RemoveItem of int

  let add x y = x + y
  let mul x y = x * y

  let three = add 3 4 |> mul 3 |> add 2 |> add 3


module Test733 =
  type Actions =
    | AddItem of string
    | RemoveItem of int

  type Actions2 =
    | AddItem of string
    | RemoveItem of int

  type Actions3 =
    | AddItem of string
    | RemoveItem of int

  let add x y = x + y
  let mul x y = x * y

  let three = add 3 4 |> mul 3 |> add 2 |> add 3


module Test734 =
  type Actions =
    | AddItem of string
    | RemoveItem of int

  type Actions2 =
    | AddItem of string
    | RemoveItem of int

  type Actions3 =
    | AddItem of string
    | RemoveItem of int

  let add x y = x + y
  let mul x y = x * y

  let three = add 3 4 |> mul 3 |> add 2 |> add 3


module Test735 =
  type Actions =
    | AddItem of string
    | RemoveItem of int

  type Actions2 =
    | AddItem of string
    | RemoveItem of int

  type Actions3 =
    | AddItem of string
    | RemoveItem of int

  let add x y = x + y
  let mul x y = x * y

  let three = add 3 4 |> mul 3 |> add 2 |> add 3


module Test736 =
  type Actions =
    | AddItem of string
    | RemoveItem of int

  type Actions2 =
    | AddItem of string
    | RemoveItem of int

  type Actions3 =
    | AddItem of string
    | RemoveItem of int

  let add x y = x + y
  let mul x y = x * y

  let three = add 3 4 |> mul 3 |> add 2 |> add 3


module Test737 =
  type Actions =
    | AddItem of string
    | RemoveItem of int

  type Actions2 =
    | AddItem of string
    | RemoveItem of int

  type Actions3 =
    | AddItem of string
    | RemoveItem of int

  let add x y = x + y
  let mul x y = x * y

  let three = add 3 4 |> mul 3 |> add 2 |> add 3


module Test738 =
  type Actions =
    | AddItem of string
    | RemoveItem of int

  type Actions2 =
    | AddItem of string
    | RemoveItem of int

  type Actions3 =
    | AddItem of string
    | RemoveItem of int

  let add x y = x + y
  let mul x y = x * y

  let three = add 3 4 |> mul 3 |> add 2 |> add 3


module Test739 =
  type Actions =
    | AddItem of string
    | RemoveItem of int

  type Actions2 =
    | AddItem of string
    | RemoveItem of int

  type Actions3 =
    | AddItem of string
    | RemoveItem of int

  let add x y = x + y
  let mul x y = x * y

  let three = add 3 4 |> mul 3 |> add 2 |> add 3


module Test7310 =
  type Actions =
    | AddItem of string
    | RemoveItem of int

  type Actions2 =
    | AddItem of string
    | RemoveItem of int

  type Actions3 =
    | AddItem of string
    | RemoveItem of int

  let add x y = x + y
  let mul x y = x * y

  let three = add 3 4 |> mul 3 |> add 2 |> add 3


module Test7311 =
  type Actions =
    | AddItem of string
    | RemoveItem of int

  type Actions2 =
    | AddItem of string
    | RemoveItem of int

  type Actions3 =
    | AddItem of string
    | RemoveItem of int

  let add x y = x + y
  let mul x y = x * y

  let three = add 3 4 |> mul 3 |> add 2 |> add 3


module Test7312 =
  type Actions =
    | AddItem of string
    | RemoveItem of int

  type Actions2 =
    | AddItem of string
    | RemoveItem of int

  type Actions3 =
    | AddItem of string
    | RemoveItem of int

  let add x y = x + y
  let mul x y = x * y

  let three = add 3 4 |> mul 3 |> add 2 |> add 3


module Test7313 =
  type Actions =
    | AddItem of string
    | RemoveItem of int

  type Actions2 =
    | AddItem of string
    | RemoveItem of int

  type Actions3 =
    | AddItem of string
    | RemoveItem of int

  let add x y = x + y
  let mul x y = x * y

  let three = add 3 4 |> mul 3 |> add 2 |> add 3


module Test7314 =
  type Actions =
    | AddItem of string
    | RemoveItem of int

  type Actions2 =
    | AddItem of string
    | RemoveItem of int

  type Actions3 =
    | AddItem of string
    | RemoveItem of int

  let add x y = x + y
  let mul x y = x * y

  let three = add 3 4 |> mul 3 |> add 2 |> add 3


module Test7315 =
  type Actions =
    | AddItem of string
    | RemoveItem of int

  type Actions2 =
    | AddItem of string
    | RemoveItem of int

  type Actions3 =
    | AddItem of string
    | RemoveItem of int

  let add x y = x + y
  let mul x y = x * y

  let three = add 3 4 |> mul 3 |> add 2 |> add 3


module Test7316 =
  type Actions =
    | AddItem of string
    | RemoveItem of int

  type Actions2 =
    | AddItem of string
    | RemoveItem of int

  type Actions3 =
    | AddItem of string
    | RemoveItem of int

  let add x y = x + y
  let mul x y = x * y

  let three = add 3 4 |> mul 3 |> add 2 |> add 3


module Test7317 =
  type Actions =
    | AddItem of string
    | RemoveItem of int

  type Actions2 =
    | AddItem of string
    | RemoveItem of int

  type Actions3 =
    | AddItem of string
    | RemoveItem of int

  let add x y = x + y
  let mul x y = x * y

  let three = add 3 4 |> mul 3 |> add 2 |> add 3


module Test7318 =
  type Actions =
    | AddItem of string
    | RemoveItem of int

  type Actions2 =
    | AddItem of string
    | RemoveItem of int

  type Actions3 =
    | AddItem of string
    | RemoveItem of int

  let add x y = x + y
  let mul x y = x * y

  let three = add 3 4 |> mul 3 |> add 2 |> add 3


module Test7319 =
  type Actions =
    | AddItem of string
    | RemoveItem of int

  type Actions2 =
    | AddItem of string
    | RemoveItem of int

  type Actions3 =
    | AddItem of string
    | RemoveItem of int

  let add x y = x + y
  let mul x y = x * y

  let three = add 3 4 |> mul 3 |> add 2 |> add 3


module Test7320 =
  type Actions =
    | AddItem of string
    | RemoveItem of int

  type Actions2 =
    | AddItem of string
    | RemoveItem of int

  type Actions3 =
    | AddItem of string
    | RemoveItem of int

  let add x y = x + y
  let mul x y = x * y

  let three = add 3 4 |> mul 3 |> add 2 |> add 3


module Test7321 =
  type Actions =
    | AddItem of string
    | RemoveItem of int

  type Actions2 =
    | AddItem of string
    | RemoveItem of int

  type Actions3 =
    | AddItem of string
    | RemoveItem of int

  let add x y = x + y
  let mul x y = x * y

  let three = add 3 4 |> mul 3 |> add 2 |> add 3


module Test7322 =
  type Actions =
    | AddItem of string
    | RemoveItem of int

  type Actions2 =
    | AddItem of string
    | RemoveItem of int

  type Actions3 =
    | AddItem of string
    | RemoveItem of int

  let add x y = x + y
  let mul x y = x * y

  let three = add 3 4 |> mul 3 |> add 2 |> add 3


module Test7323 =
  type Actions =
    | AddItem of string
    | RemoveItem of int

  type Actions2 =
    | AddItem of string
    | RemoveItem of int

  type Actions3 =
    | AddItem of string
    | RemoveItem of int

  let add x y = x + y
  let mul x y = x * y

  let three = add 3 4 |> mul 3 |> add 2 |> add 3


module Test7324 =
  type Actions =
    | AddItem of string
    | RemoveItem of int

  type Actions2 =
    | AddItem of string
    | RemoveItem of int

  type Actions3 =
    | AddItem of string
    | RemoveItem of int

  let add x y = x + y
  let mul x y = x * y

  let three = add 3 4 |> mul 3 |> add 2 |> add 3


module Test7325 =
  type Actions =
    | AddItem of string
    | RemoveItem of int

  type Actions2 =
    | AddItem of string
    | RemoveItem of int

  type Actions3 =
    | AddItem of string
    | RemoveItem of int

  let add x y = x + y
  let mul x y = x * y

  let three = add 3 4 |> mul 3 |> add 2 |> add 3


module Test7326 =
  type Actions =
    | AddItem of string
    | RemoveItem of int

  type Actions2 =
    | AddItem of string
    | RemoveItem of int

  type Actions3 =
    | AddItem of string
    | RemoveItem of int

  let add x y = x + y
  let mul x y = x * y

  let three = add 3 4 |> mul 3 |> add 2 |> add 3


module Test7327 =
  type Actions =
    | AddItem of string
    | RemoveItem of int

  type Actions2 =
    | AddItem of string
    | RemoveItem of int

  type Actions3 =
    | AddItem of string
    | RemoveItem of int

  let add x y = x + y
  let mul x y = x * y

  let three = add 3 4 |> mul 3 |> add 2 |> add 3


module Test7328 =
  type Actions =
    | AddItem of string
    | RemoveItem of int

  type Actions2 =
    | AddItem of string
    | RemoveItem of int

  type Actions3 =
    | AddItem of string
    | RemoveItem of int

  let add x y = x + y
  let mul x y = x * y

  let three = add 3 4 |> mul 3 |> add 2 |> add 3


module Test7329 =
  type Actions =
    | AddItem of string
    | RemoveItem of int

  type Actions2 =
    | AddItem of string
    | RemoveItem of int

  type Actions3 =
    | AddItem of string
    | RemoveItem of int

  let add x y = x + y
  let mul x y = x * y

  let three = add 3 4 |> mul 3 |> add 2 |> add 3


module Test7330 =
  type Actions =
    | AddItem of string
    | RemoveItem of int

  type Actions2 =
    | AddItem of string
    | RemoveItem of int

  type Actions3 =
    | AddItem of string
    | RemoveItem of int

  let add x y = x + y
  let mul x y = x * y

  let three = add 3 4 |> mul 3 |> add 2 |> add 3


module Test7331 =
  type Actions =
    | AddItem of string
    | RemoveItem of int

  type Actions2 =
    | AddItem of string
    | RemoveItem of int

  type Actions3 =
    | AddItem of string
    | RemoveItem of int

  let add x y = x + y
  let mul x y = x * y

  let three = add 3 4 |> mul 3 |> add 2 |> add 3


module Test7332 =
  type Actions =
    | AddItem of string
    | RemoveItem of int

  type Actions2 =
    | AddItem of string
    | RemoveItem of int

  type Actions3 =
    | AddItem of string
    | RemoveItem of int

  let add x y = x + y
  let mul x y = x * y

  let three = add 3 4 |> mul 3 |> add 2 |> add 3


module Test7333 =
  type Actions =
    | AddItem of string
    | RemoveItem of int

  type Actions2 =
    | AddItem of string
    | RemoveItem of int

  type Actions3 =
    | AddItem of string
    | RemoveItem of int

  let add x y = x + y
  let mul x y = x * y

  let three = add 3 4 |> mul 3 |> add 2 |> add 3


module Test7334 =
  type Actions =
    | AddItem of string
    | RemoveItem of int

  type Actions2 =
    | AddItem of string
    | RemoveItem of int

  type Actions3 =
    | AddItem of string
    | RemoveItem of int

  let add x y = x + y
  let mul x y = x * y

  let three = add 3 4 |> mul 3 |> add 2 |> add 3


module Test7335 =
  type Actions =
    | AddItem of string
    | RemoveItem of int

  type Actions2 =
    | AddItem of string
    | RemoveItem of int

  type Actions3 =
    | AddItem of string
    | RemoveItem of int

  let add x y = x + y
  let mul x y = x * y

  let three = add 3 4 |> mul 3 |> add 2 |> add 3


module Test7336 =
  type Actions =
    | AddItem of string
    | RemoveItem of int

  type Actions2 =
    | AddItem of string
    | RemoveItem of int

  type Actions3 =
    | AddItem of string
    | RemoveItem of int

  let add x y = x + y
  let mul x y = x * y

  let three = add 3 4 |> mul 3 |> add 2 |> add 3


module Test7337 =
  type Actions =
    | AddItem of string
    | RemoveItem of int

  type Actions2 =
    | AddItem of string
    | RemoveItem of int

  type Actions3 =
    | AddItem of string
    | RemoveItem of int

  let add x y = x + y
  let mul x y = x * y

  let three = add 3 4 |> mul 3 |> add 2 |> add 3


module Test7338 =
  type Actions =
    | AddItem of string
    | RemoveItem of int

  type Actions2 =
    | AddItem of string
    | RemoveItem of int

  type Actions3 =
    | AddItem of string
    | RemoveItem of int

  let add x y = x + y
  let mul x y = x * y

  let three = add 3 4 |> mul 3 |> add 2 |> add 3


module Test7339 =
  type Actions =
    | AddItem of string
    | RemoveItem of int

  type Actions2 =
    | AddItem of string
    | RemoveItem of int

  type Actions3 =
    | AddItem of string
    | RemoveItem of int

  let add x y = x + y
  let mul x y = x * y

  let three = add 3 4 |> mul 3 |> add 2 |> add 3


module Test7340 =
  type Actions =
    | AddItem of string
    | RemoveItem of int

  type Actions2 =
    | AddItem of string
    | RemoveItem of int

  type Actions3 =
    | AddItem of string
    | RemoveItem of int

  let add x y = x + y
  let mul x y = x * y

  let three = add 3 4 |> mul 3 |> add 2 |> add 3


module Test7341 =
  type Actions =
    | AddItem of string
    | RemoveItem of int

  type Actions2 =
    | AddItem of string
    | RemoveItem of int

  type Actions3 =
    | AddItem of string
    | RemoveItem of int

  let add x y = x + y
  let mul x y = x * y

  let three = add 3 4 |> mul 3 |> add 2 |> add 3


module Test7342 =
  type Actions =
    | AddItem of string
    | RemoveItem of int

  type Actions2 =
    | AddItem of string
    | RemoveItem of int

  type Actions3 =
    | AddItem of string
    | RemoveItem of int

  let add x y = x + y
  let mul x y = x * y

  let three = add 3 4 |> mul 3 |> add 2 |> add 3


module Test7343 =
  type Actions =
    | AddItem of string
    | RemoveItem of int

  type Actions2 =
    | AddItem of string
    | RemoveItem of int

  type Actions3 =
    | AddItem of string
    | RemoveItem of int

  let add x y = x + y
  let mul x y = x * y

  let three = add 3 4 |> mul 3 |> add 2 |> add 3


module Test7344 =
  type Actions =
    | AddItem of string
    | RemoveItem of int

  type Actions2 =
    | AddItem of string
    | RemoveItem of int

  type Actions3 =
    | AddItem of string
    | RemoveItem of int

  let add x y = x + y
  let mul x y = x * y

  let three = add 3 4 |> mul 3 |> add 2 |> add 3


module Test7345 =
  type Actions =
    | AddItem of string
    | RemoveItem of int

  type Actions2 =
    | AddItem of string
    | RemoveItem of int

  type Actions3 =
    | AddItem of string
    | RemoveItem of int

  let add x y = x + y
  let mul x y = x * y

  let three = add 3 4 |> mul 3 |> add 2 |> add 3


module Test7346 =
  type Actions =
    | AddItem of string
    | RemoveItem of int

  type Actions2 =
    | AddItem of string
    | RemoveItem of int

  type Actions3 =
    | AddItem of string
    | RemoveItem of int

  let add x y = x + y
  let mul x y = x * y

  let three = add 3 4 |> mul 3 |> add 2 |> add 3


module Test7347 =
  type Actions =
    | AddItem of string
    | RemoveItem of int

  type Actions2 =
    | AddItem of string
    | RemoveItem of int

  type Actions3 =
    | AddItem of string
    | RemoveItem of int

  let add x y = x + y
  let mul x y = x * y

  let three = add 3 4 |> mul 3 |> add 2 |> add 3


module Test7348 =
  type Actions =
    | AddItem of string
    | RemoveItem of int

  type Actions2 =
    | AddItem of string
    | RemoveItem of int

  type Actions3 =
    | AddItem of string
    | RemoveItem of int

  let add x y = x + y
  let mul x y = x * y

  let three = add 3 4 |> mul 3 |> add 2 |> add 3


module Test7349 =
  type Actions =
    | AddItem of string
    | RemoveItem of int

  type Actions2 =
    | AddItem of string
    | RemoveItem of int

  type Actions3 =
    | AddItem of string
    | RemoveItem of int

  let add x y = x + y
  let mul x y = x * y

  let three = add 3 4 |> mul 3 |> add 2 |> add 3


module Test7350 =
  type Actions =
    | AddItem of string
    | RemoveItem of int

  type Actions2 =
    | AddItem of string
    | RemoveItem of int

  type Actions3 =
    | AddItem of string
    | RemoveItem of int

  let add x y = x + y
  let mul x y = x * y

  let three = add 3 4 |> mul 3 |> add 2 |> add 3

