module Test80
module Test801 =
  type Actions =
    | AddItem of string
    | RemoveItem of int

  type Actions2 =
    | AddItem of string
    | RemoveItem of int

  type Actions3 =
    | AddItem of string
    | RemoveItem of int

  let add x y = x + y
  let mul x y = x * y

  let three = add 3 4 |> mul 3 |> add 2 |> add 3


module Test802 =
  type Actions =
    | AddItem of string
    | RemoveItem of int

  type Actions2 =
    | AddItem of string
    | RemoveItem of int

  type Actions3 =
    | AddItem of string
    | RemoveItem of int

  let add x y = x + y
  let mul x y = x * y

  let three = add 3 4 |> mul 3 |> add 2 |> add 3


module Test803 =
  type Actions =
    | AddItem of string
    | RemoveItem of int

  type Actions2 =
    | AddItem of string
    | RemoveItem of int

  type Actions3 =
    | AddItem of string
    | RemoveItem of int

  let add x y = x + y
  let mul x y = x * y

  let three = add 3 4 |> mul 3 |> add 2 |> add 3


module Test804 =
  type Actions =
    | AddItem of string
    | RemoveItem of int

  type Actions2 =
    | AddItem of string
    | RemoveItem of int

  type Actions3 =
    | AddItem of string
    | RemoveItem of int

  let add x y = x + y
  let mul x y = x * y

  let three = add 3 4 |> mul 3 |> add 2 |> add 3


module Test805 =
  type Actions =
    | AddItem of string
    | RemoveItem of int

  type Actions2 =
    | AddItem of string
    | RemoveItem of int

  type Actions3 =
    | AddItem of string
    | RemoveItem of int

  let add x y = x + y
  let mul x y = x * y

  let three = add 3 4 |> mul 3 |> add 2 |> add 3


module Test806 =
  type Actions =
    | AddItem of string
    | RemoveItem of int

  type Actions2 =
    | AddItem of string
    | RemoveItem of int

  type Actions3 =
    | AddItem of string
    | RemoveItem of int

  let add x y = x + y
  let mul x y = x * y

  let three = add 3 4 |> mul 3 |> add 2 |> add 3


module Test807 =
  type Actions =
    | AddItem of string
    | RemoveItem of int

  type Actions2 =
    | AddItem of string
    | RemoveItem of int

  type Actions3 =
    | AddItem of string
    | RemoveItem of int

  let add x y = x + y
  let mul x y = x * y

  let three = add 3 4 |> mul 3 |> add 2 |> add 3


module Test808 =
  type Actions =
    | AddItem of string
    | RemoveItem of int

  type Actions2 =
    | AddItem of string
    | RemoveItem of int

  type Actions3 =
    | AddItem of string
    | RemoveItem of int

  let add x y = x + y
  let mul x y = x * y

  let three = add 3 4 |> mul 3 |> add 2 |> add 3


module Test809 =
  type Actions =
    | AddItem of string
    | RemoveItem of int

  type Actions2 =
    | AddItem of string
    | RemoveItem of int

  type Actions3 =
    | AddItem of string
    | RemoveItem of int

  let add x y = x + y
  let mul x y = x * y

  let three = add 3 4 |> mul 3 |> add 2 |> add 3


module Test8010 =
  type Actions =
    | AddItem of string
    | RemoveItem of int

  type Actions2 =
    | AddItem of string
    | RemoveItem of int

  type Actions3 =
    | AddItem of string
    | RemoveItem of int

  let add x y = x + y
  let mul x y = x * y

  let three = add 3 4 |> mul 3 |> add 2 |> add 3


module Test8011 =
  type Actions =
    | AddItem of string
    | RemoveItem of int

  type Actions2 =
    | AddItem of string
    | RemoveItem of int

  type Actions3 =
    | AddItem of string
    | RemoveItem of int

  let add x y = x + y
  let mul x y = x * y

  let three = add 3 4 |> mul 3 |> add 2 |> add 3


module Test8012 =
  type Actions =
    | AddItem of string
    | RemoveItem of int

  type Actions2 =
    | AddItem of string
    | RemoveItem of int

  type Actions3 =
    | AddItem of string
    | RemoveItem of int

  let add x y = x + y
  let mul x y = x * y

  let three = add 3 4 |> mul 3 |> add 2 |> add 3


module Test8013 =
  type Actions =
    | AddItem of string
    | RemoveItem of int

  type Actions2 =
    | AddItem of string
    | RemoveItem of int

  type Actions3 =
    | AddItem of string
    | RemoveItem of int

  let add x y = x + y
  let mul x y = x * y

  let three = add 3 4 |> mul 3 |> add 2 |> add 3


module Test8014 =
  type Actions =
    | AddItem of string
    | RemoveItem of int

  type Actions2 =
    | AddItem of string
    | RemoveItem of int

  type Actions3 =
    | AddItem of string
    | RemoveItem of int

  let add x y = x + y
  let mul x y = x * y

  let three = add 3 4 |> mul 3 |> add 2 |> add 3


module Test8015 =
  type Actions =
    | AddItem of string
    | RemoveItem of int

  type Actions2 =
    | AddItem of string
    | RemoveItem of int

  type Actions3 =
    | AddItem of string
    | RemoveItem of int

  let add x y = x + y
  let mul x y = x * y

  let three = add 3 4 |> mul 3 |> add 2 |> add 3


module Test8016 =
  type Actions =
    | AddItem of string
    | RemoveItem of int

  type Actions2 =
    | AddItem of string
    | RemoveItem of int

  type Actions3 =
    | AddItem of string
    | RemoveItem of int

  let add x y = x + y
  let mul x y = x * y

  let three = add 3 4 |> mul 3 |> add 2 |> add 3


module Test8017 =
  type Actions =
    | AddItem of string
    | RemoveItem of int

  type Actions2 =
    | AddItem of string
    | RemoveItem of int

  type Actions3 =
    | AddItem of string
    | RemoveItem of int

  let add x y = x + y
  let mul x y = x * y

  let three = add 3 4 |> mul 3 |> add 2 |> add 3


module Test8018 =
  type Actions =
    | AddItem of string
    | RemoveItem of int

  type Actions2 =
    | AddItem of string
    | RemoveItem of int

  type Actions3 =
    | AddItem of string
    | RemoveItem of int

  let add x y = x + y
  let mul x y = x * y

  let three = add 3 4 |> mul 3 |> add 2 |> add 3


module Test8019 =
  type Actions =
    | AddItem of string
    | RemoveItem of int

  type Actions2 =
    | AddItem of string
    | RemoveItem of int

  type Actions3 =
    | AddItem of string
    | RemoveItem of int

  let add x y = x + y
  let mul x y = x * y

  let three = add 3 4 |> mul 3 |> add 2 |> add 3


module Test8020 =
  type Actions =
    | AddItem of string
    | RemoveItem of int

  type Actions2 =
    | AddItem of string
    | RemoveItem of int

  type Actions3 =
    | AddItem of string
    | RemoveItem of int

  let add x y = x + y
  let mul x y = x * y

  let three = add 3 4 |> mul 3 |> add 2 |> add 3


module Test8021 =
  type Actions =
    | AddItem of string
    | RemoveItem of int

  type Actions2 =
    | AddItem of string
    | RemoveItem of int

  type Actions3 =
    | AddItem of string
    | RemoveItem of int

  let add x y = x + y
  let mul x y = x * y

  let three = add 3 4 |> mul 3 |> add 2 |> add 3


module Test8022 =
  type Actions =
    | AddItem of string
    | RemoveItem of int

  type Actions2 =
    | AddItem of string
    | RemoveItem of int

  type Actions3 =
    | AddItem of string
    | RemoveItem of int

  let add x y = x + y
  let mul x y = x * y

  let three = add 3 4 |> mul 3 |> add 2 |> add 3


module Test8023 =
  type Actions =
    | AddItem of string
    | RemoveItem of int

  type Actions2 =
    | AddItem of string
    | RemoveItem of int

  type Actions3 =
    | AddItem of string
    | RemoveItem of int

  let add x y = x + y
  let mul x y = x * y

  let three = add 3 4 |> mul 3 |> add 2 |> add 3


module Test8024 =
  type Actions =
    | AddItem of string
    | RemoveItem of int

  type Actions2 =
    | AddItem of string
    | RemoveItem of int

  type Actions3 =
    | AddItem of string
    | RemoveItem of int

  let add x y = x + y
  let mul x y = x * y

  let three = add 3 4 |> mul 3 |> add 2 |> add 3


module Test8025 =
  type Actions =
    | AddItem of string
    | RemoveItem of int

  type Actions2 =
    | AddItem of string
    | RemoveItem of int

  type Actions3 =
    | AddItem of string
    | RemoveItem of int

  let add x y = x + y
  let mul x y = x * y

  let three = add 3 4 |> mul 3 |> add 2 |> add 3


module Test8026 =
  type Actions =
    | AddItem of string
    | RemoveItem of int

  type Actions2 =
    | AddItem of string
    | RemoveItem of int

  type Actions3 =
    | AddItem of string
    | RemoveItem of int

  let add x y = x + y
  let mul x y = x * y

  let three = add 3 4 |> mul 3 |> add 2 |> add 3


module Test8027 =
  type Actions =
    | AddItem of string
    | RemoveItem of int

  type Actions2 =
    | AddItem of string
    | RemoveItem of int

  type Actions3 =
    | AddItem of string
    | RemoveItem of int

  let add x y = x + y
  let mul x y = x * y

  let three = add 3 4 |> mul 3 |> add 2 |> add 3


module Test8028 =
  type Actions =
    | AddItem of string
    | RemoveItem of int

  type Actions2 =
    | AddItem of string
    | RemoveItem of int

  type Actions3 =
    | AddItem of string
    | RemoveItem of int

  let add x y = x + y
  let mul x y = x * y

  let three = add 3 4 |> mul 3 |> add 2 |> add 3


module Test8029 =
  type Actions =
    | AddItem of string
    | RemoveItem of int

  type Actions2 =
    | AddItem of string
    | RemoveItem of int

  type Actions3 =
    | AddItem of string
    | RemoveItem of int

  let add x y = x + y
  let mul x y = x * y

  let three = add 3 4 |> mul 3 |> add 2 |> add 3


module Test8030 =
  type Actions =
    | AddItem of string
    | RemoveItem of int

  type Actions2 =
    | AddItem of string
    | RemoveItem of int

  type Actions3 =
    | AddItem of string
    | RemoveItem of int

  let add x y = x + y
  let mul x y = x * y

  let three = add 3 4 |> mul 3 |> add 2 |> add 3


module Test8031 =
  type Actions =
    | AddItem of string
    | RemoveItem of int

  type Actions2 =
    | AddItem of string
    | RemoveItem of int

  type Actions3 =
    | AddItem of string
    | RemoveItem of int

  let add x y = x + y
  let mul x y = x * y

  let three = add 3 4 |> mul 3 |> add 2 |> add 3


module Test8032 =
  type Actions =
    | AddItem of string
    | RemoveItem of int

  type Actions2 =
    | AddItem of string
    | RemoveItem of int

  type Actions3 =
    | AddItem of string
    | RemoveItem of int

  let add x y = x + y
  let mul x y = x * y

  let three = add 3 4 |> mul 3 |> add 2 |> add 3


module Test8033 =
  type Actions =
    | AddItem of string
    | RemoveItem of int

  type Actions2 =
    | AddItem of string
    | RemoveItem of int

  type Actions3 =
    | AddItem of string
    | RemoveItem of int

  let add x y = x + y
  let mul x y = x * y

  let three = add 3 4 |> mul 3 |> add 2 |> add 3


module Test8034 =
  type Actions =
    | AddItem of string
    | RemoveItem of int

  type Actions2 =
    | AddItem of string
    | RemoveItem of int

  type Actions3 =
    | AddItem of string
    | RemoveItem of int

  let add x y = x + y
  let mul x y = x * y

  let three = add 3 4 |> mul 3 |> add 2 |> add 3


module Test8035 =
  type Actions =
    | AddItem of string
    | RemoveItem of int

  type Actions2 =
    | AddItem of string
    | RemoveItem of int

  type Actions3 =
    | AddItem of string
    | RemoveItem of int

  let add x y = x + y
  let mul x y = x * y

  let three = add 3 4 |> mul 3 |> add 2 |> add 3


module Test8036 =
  type Actions =
    | AddItem of string
    | RemoveItem of int

  type Actions2 =
    | AddItem of string
    | RemoveItem of int

  type Actions3 =
    | AddItem of string
    | RemoveItem of int

  let add x y = x + y
  let mul x y = x * y

  let three = add 3 4 |> mul 3 |> add 2 |> add 3


module Test8037 =
  type Actions =
    | AddItem of string
    | RemoveItem of int

  type Actions2 =
    | AddItem of string
    | RemoveItem of int

  type Actions3 =
    | AddItem of string
    | RemoveItem of int

  let add x y = x + y
  let mul x y = x * y

  let three = add 3 4 |> mul 3 |> add 2 |> add 3


module Test8038 =
  type Actions =
    | AddItem of string
    | RemoveItem of int

  type Actions2 =
    | AddItem of string
    | RemoveItem of int

  type Actions3 =
    | AddItem of string
    | RemoveItem of int

  let add x y = x + y
  let mul x y = x * y

  let three = add 3 4 |> mul 3 |> add 2 |> add 3


module Test8039 =
  type Actions =
    | AddItem of string
    | RemoveItem of int

  type Actions2 =
    | AddItem of string
    | RemoveItem of int

  type Actions3 =
    | AddItem of string
    | RemoveItem of int

  let add x y = x + y
  let mul x y = x * y

  let three = add 3 4 |> mul 3 |> add 2 |> add 3


module Test8040 =
  type Actions =
    | AddItem of string
    | RemoveItem of int

  type Actions2 =
    | AddItem of string
    | RemoveItem of int

  type Actions3 =
    | AddItem of string
    | RemoveItem of int

  let add x y = x + y
  let mul x y = x * y

  let three = add 3 4 |> mul 3 |> add 2 |> add 3


module Test8041 =
  type Actions =
    | AddItem of string
    | RemoveItem of int

  type Actions2 =
    | AddItem of string
    | RemoveItem of int

  type Actions3 =
    | AddItem of string
    | RemoveItem of int

  let add x y = x + y
  let mul x y = x * y

  let three = add 3 4 |> mul 3 |> add 2 |> add 3


module Test8042 =
  type Actions =
    | AddItem of string
    | RemoveItem of int

  type Actions2 =
    | AddItem of string
    | RemoveItem of int

  type Actions3 =
    | AddItem of string
    | RemoveItem of int

  let add x y = x + y
  let mul x y = x * y

  let three = add 3 4 |> mul 3 |> add 2 |> add 3


module Test8043 =
  type Actions =
    | AddItem of string
    | RemoveItem of int

  type Actions2 =
    | AddItem of string
    | RemoveItem of int

  type Actions3 =
    | AddItem of string
    | RemoveItem of int

  let add x y = x + y
  let mul x y = x * y

  let three = add 3 4 |> mul 3 |> add 2 |> add 3


module Test8044 =
  type Actions =
    | AddItem of string
    | RemoveItem of int

  type Actions2 =
    | AddItem of string
    | RemoveItem of int

  type Actions3 =
    | AddItem of string
    | RemoveItem of int

  let add x y = x + y
  let mul x y = x * y

  let three = add 3 4 |> mul 3 |> add 2 |> add 3


module Test8045 =
  type Actions =
    | AddItem of string
    | RemoveItem of int

  type Actions2 =
    | AddItem of string
    | RemoveItem of int

  type Actions3 =
    | AddItem of string
    | RemoveItem of int

  let add x y = x + y
  let mul x y = x * y

  let three = add 3 4 |> mul 3 |> add 2 |> add 3


module Test8046 =
  type Actions =
    | AddItem of string
    | RemoveItem of int

  type Actions2 =
    | AddItem of string
    | RemoveItem of int

  type Actions3 =
    | AddItem of string
    | RemoveItem of int

  let add x y = x + y
  let mul x y = x * y

  let three = add 3 4 |> mul 3 |> add 2 |> add 3


module Test8047 =
  type Actions =
    | AddItem of string
    | RemoveItem of int

  type Actions2 =
    | AddItem of string
    | RemoveItem of int

  type Actions3 =
    | AddItem of string
    | RemoveItem of int

  let add x y = x + y
  let mul x y = x * y

  let three = add 3 4 |> mul 3 |> add 2 |> add 3


module Test8048 =
  type Actions =
    | AddItem of string
    | RemoveItem of int

  type Actions2 =
    | AddItem of string
    | RemoveItem of int

  type Actions3 =
    | AddItem of string
    | RemoveItem of int

  let add x y = x + y
  let mul x y = x * y

  let three = add 3 4 |> mul 3 |> add 2 |> add 3


module Test8049 =
  type Actions =
    | AddItem of string
    | RemoveItem of int

  type Actions2 =
    | AddItem of string
    | RemoveItem of int

  type Actions3 =
    | AddItem of string
    | RemoveItem of int

  let add x y = x + y
  let mul x y = x * y

  let three = add 3 4 |> mul 3 |> add 2 |> add 3


module Test8050 =
  type Actions =
    | AddItem of string
    | RemoveItem of int

  type Actions2 =
    | AddItem of string
    | RemoveItem of int

  type Actions3 =
    | AddItem of string
    | RemoveItem of int

  let add x y = x + y
  let mul x y = x * y

  let three = add 3 4 |> mul 3 |> add 2 |> add 3

