module Test25
module Test251 =
  type Actions =
    | AddItem of string
    | RemoveItem of int

  type Actions2 =
    | AddItem of string
    | RemoveItem of int

  type Actions3 =
    | AddItem of string
    | RemoveItem of int

  let add x y = x + y
  let mul x y = x * y

  let three = add 3 4 |> mul 3 |> add 2 |> add 3


module Test252 =
  type Actions =
    | AddItem of string
    | RemoveItem of int

  type Actions2 =
    | AddItem of string
    | RemoveItem of int

  type Actions3 =
    | AddItem of string
    | RemoveItem of int

  let add x y = x + y
  let mul x y = x * y

  let three = add 3 4 |> mul 3 |> add 2 |> add 3


module Test253 =
  type Actions =
    | AddItem of string
    | RemoveItem of int

  type Actions2 =
    | AddItem of string
    | RemoveItem of int

  type Actions3 =
    | AddItem of string
    | RemoveItem of int

  let add x y = x + y
  let mul x y = x * y

  let three = add 3 4 |> mul 3 |> add 2 |> add 3


module Test254 =
  type Actions =
    | AddItem of string
    | RemoveItem of int

  type Actions2 =
    | AddItem of string
    | RemoveItem of int

  type Actions3 =
    | AddItem of string
    | RemoveItem of int

  let add x y = x + y
  let mul x y = x * y

  let three = add 3 4 |> mul 3 |> add 2 |> add 3


module Test255 =
  type Actions =
    | AddItem of string
    | RemoveItem of int

  type Actions2 =
    | AddItem of string
    | RemoveItem of int

  type Actions3 =
    | AddItem of string
    | RemoveItem of int

  let add x y = x + y
  let mul x y = x * y

  let three = add 3 4 |> mul 3 |> add 2 |> add 3


module Test256 =
  type Actions =
    | AddItem of string
    | RemoveItem of int

  type Actions2 =
    | AddItem of string
    | RemoveItem of int

  type Actions3 =
    | AddItem of string
    | RemoveItem of int

  let add x y = x + y
  let mul x y = x * y

  let three = add 3 4 |> mul 3 |> add 2 |> add 3


module Test257 =
  type Actions =
    | AddItem of string
    | RemoveItem of int

  type Actions2 =
    | AddItem of string
    | RemoveItem of int

  type Actions3 =
    | AddItem of string
    | RemoveItem of int

  let add x y = x + y
  let mul x y = x * y

  let three = add 3 4 |> mul 3 |> add 2 |> add 3


module Test258 =
  type Actions =
    | AddItem of string
    | RemoveItem of int

  type Actions2 =
    | AddItem of string
    | RemoveItem of int

  type Actions3 =
    | AddItem of string
    | RemoveItem of int

  let add x y = x + y
  let mul x y = x * y

  let three = add 3 4 |> mul 3 |> add 2 |> add 3


module Test259 =
  type Actions =
    | AddItem of string
    | RemoveItem of int

  type Actions2 =
    | AddItem of string
    | RemoveItem of int

  type Actions3 =
    | AddItem of string
    | RemoveItem of int

  let add x y = x + y
  let mul x y = x * y

  let three = add 3 4 |> mul 3 |> add 2 |> add 3


module Test2510 =
  type Actions =
    | AddItem of string
    | RemoveItem of int

  type Actions2 =
    | AddItem of string
    | RemoveItem of int

  type Actions3 =
    | AddItem of string
    | RemoveItem of int

  let add x y = x + y
  let mul x y = x * y

  let three = add 3 4 |> mul 3 |> add 2 |> add 3


module Test2511 =
  type Actions =
    | AddItem of string
    | RemoveItem of int

  type Actions2 =
    | AddItem of string
    | RemoveItem of int

  type Actions3 =
    | AddItem of string
    | RemoveItem of int

  let add x y = x + y
  let mul x y = x * y

  let three = add 3 4 |> mul 3 |> add 2 |> add 3


module Test2512 =
  type Actions =
    | AddItem of string
    | RemoveItem of int

  type Actions2 =
    | AddItem of string
    | RemoveItem of int

  type Actions3 =
    | AddItem of string
    | RemoveItem of int

  let add x y = x + y
  let mul x y = x * y

  let three = add 3 4 |> mul 3 |> add 2 |> add 3


module Test2513 =
  type Actions =
    | AddItem of string
    | RemoveItem of int

  type Actions2 =
    | AddItem of string
    | RemoveItem of int

  type Actions3 =
    | AddItem of string
    | RemoveItem of int

  let add x y = x + y
  let mul x y = x * y

  let three = add 3 4 |> mul 3 |> add 2 |> add 3


module Test2514 =
  type Actions =
    | AddItem of string
    | RemoveItem of int

  type Actions2 =
    | AddItem of string
    | RemoveItem of int

  type Actions3 =
    | AddItem of string
    | RemoveItem of int

  let add x y = x + y
  let mul x y = x * y

  let three = add 3 4 |> mul 3 |> add 2 |> add 3


module Test2515 =
  type Actions =
    | AddItem of string
    | RemoveItem of int

  type Actions2 =
    | AddItem of string
    | RemoveItem of int

  type Actions3 =
    | AddItem of string
    | RemoveItem of int

  let add x y = x + y
  let mul x y = x * y

  let three = add 3 4 |> mul 3 |> add 2 |> add 3


module Test2516 =
  type Actions =
    | AddItem of string
    | RemoveItem of int

  type Actions2 =
    | AddItem of string
    | RemoveItem of int

  type Actions3 =
    | AddItem of string
    | RemoveItem of int

  let add x y = x + y
  let mul x y = x * y

  let three = add 3 4 |> mul 3 |> add 2 |> add 3


module Test2517 =
  type Actions =
    | AddItem of string
    | RemoveItem of int

  type Actions2 =
    | AddItem of string
    | RemoveItem of int

  type Actions3 =
    | AddItem of string
    | RemoveItem of int

  let add x y = x + y
  let mul x y = x * y

  let three = add 3 4 |> mul 3 |> add 2 |> add 3


module Test2518 =
  type Actions =
    | AddItem of string
    | RemoveItem of int

  type Actions2 =
    | AddItem of string
    | RemoveItem of int

  type Actions3 =
    | AddItem of string
    | RemoveItem of int

  let add x y = x + y
  let mul x y = x * y

  let three = add 3 4 |> mul 3 |> add 2 |> add 3


module Test2519 =
  type Actions =
    | AddItem of string
    | RemoveItem of int

  type Actions2 =
    | AddItem of string
    | RemoveItem of int

  type Actions3 =
    | AddItem of string
    | RemoveItem of int

  let add x y = x + y
  let mul x y = x * y

  let three = add 3 4 |> mul 3 |> add 2 |> add 3


module Test2520 =
  type Actions =
    | AddItem of string
    | RemoveItem of int

  type Actions2 =
    | AddItem of string
    | RemoveItem of int

  type Actions3 =
    | AddItem of string
    | RemoveItem of int

  let add x y = x + y
  let mul x y = x * y

  let three = add 3 4 |> mul 3 |> add 2 |> add 3


module Test2521 =
  type Actions =
    | AddItem of string
    | RemoveItem of int

  type Actions2 =
    | AddItem of string
    | RemoveItem of int

  type Actions3 =
    | AddItem of string
    | RemoveItem of int

  let add x y = x + y
  let mul x y = x * y

  let three = add 3 4 |> mul 3 |> add 2 |> add 3


module Test2522 =
  type Actions =
    | AddItem of string
    | RemoveItem of int

  type Actions2 =
    | AddItem of string
    | RemoveItem of int

  type Actions3 =
    | AddItem of string
    | RemoveItem of int

  let add x y = x + y
  let mul x y = x * y

  let three = add 3 4 |> mul 3 |> add 2 |> add 3


module Test2523 =
  type Actions =
    | AddItem of string
    | RemoveItem of int

  type Actions2 =
    | AddItem of string
    | RemoveItem of int

  type Actions3 =
    | AddItem of string
    | RemoveItem of int

  let add x y = x + y
  let mul x y = x * y

  let three = add 3 4 |> mul 3 |> add 2 |> add 3


module Test2524 =
  type Actions =
    | AddItem of string
    | RemoveItem of int

  type Actions2 =
    | AddItem of string
    | RemoveItem of int

  type Actions3 =
    | AddItem of string
    | RemoveItem of int

  let add x y = x + y
  let mul x y = x * y

  let three = add 3 4 |> mul 3 |> add 2 |> add 3


module Test2525 =
  type Actions =
    | AddItem of string
    | RemoveItem of int

  type Actions2 =
    | AddItem of string
    | RemoveItem of int

  type Actions3 =
    | AddItem of string
    | RemoveItem of int

  let add x y = x + y
  let mul x y = x * y

  let three = add 3 4 |> mul 3 |> add 2 |> add 3


module Test2526 =
  type Actions =
    | AddItem of string
    | RemoveItem of int

  type Actions2 =
    | AddItem of string
    | RemoveItem of int

  type Actions3 =
    | AddItem of string
    | RemoveItem of int

  let add x y = x + y
  let mul x y = x * y

  let three = add 3 4 |> mul 3 |> add 2 |> add 3


module Test2527 =
  type Actions =
    | AddItem of string
    | RemoveItem of int

  type Actions2 =
    | AddItem of string
    | RemoveItem of int

  type Actions3 =
    | AddItem of string
    | RemoveItem of int

  let add x y = x + y
  let mul x y = x * y

  let three = add 3 4 |> mul 3 |> add 2 |> add 3


module Test2528 =
  type Actions =
    | AddItem of string
    | RemoveItem of int

  type Actions2 =
    | AddItem of string
    | RemoveItem of int

  type Actions3 =
    | AddItem of string
    | RemoveItem of int

  let add x y = x + y
  let mul x y = x * y

  let three = add 3 4 |> mul 3 |> add 2 |> add 3


module Test2529 =
  type Actions =
    | AddItem of string
    | RemoveItem of int

  type Actions2 =
    | AddItem of string
    | RemoveItem of int

  type Actions3 =
    | AddItem of string
    | RemoveItem of int

  let add x y = x + y
  let mul x y = x * y

  let three = add 3 4 |> mul 3 |> add 2 |> add 3


module Test2530 =
  type Actions =
    | AddItem of string
    | RemoveItem of int

  type Actions2 =
    | AddItem of string
    | RemoveItem of int

  type Actions3 =
    | AddItem of string
    | RemoveItem of int

  let add x y = x + y
  let mul x y = x * y

  let three = add 3 4 |> mul 3 |> add 2 |> add 3


module Test2531 =
  type Actions =
    | AddItem of string
    | RemoveItem of int

  type Actions2 =
    | AddItem of string
    | RemoveItem of int

  type Actions3 =
    | AddItem of string
    | RemoveItem of int

  let add x y = x + y
  let mul x y = x * y

  let three = add 3 4 |> mul 3 |> add 2 |> add 3


module Test2532 =
  type Actions =
    | AddItem of string
    | RemoveItem of int

  type Actions2 =
    | AddItem of string
    | RemoveItem of int

  type Actions3 =
    | AddItem of string
    | RemoveItem of int

  let add x y = x + y
  let mul x y = x * y

  let three = add 3 4 |> mul 3 |> add 2 |> add 3


module Test2533 =
  type Actions =
    | AddItem of string
    | RemoveItem of int

  type Actions2 =
    | AddItem of string
    | RemoveItem of int

  type Actions3 =
    | AddItem of string
    | RemoveItem of int

  let add x y = x + y
  let mul x y = x * y

  let three = add 3 4 |> mul 3 |> add 2 |> add 3


module Test2534 =
  type Actions =
    | AddItem of string
    | RemoveItem of int

  type Actions2 =
    | AddItem of string
    | RemoveItem of int

  type Actions3 =
    | AddItem of string
    | RemoveItem of int

  let add x y = x + y
  let mul x y = x * y

  let three = add 3 4 |> mul 3 |> add 2 |> add 3


module Test2535 =
  type Actions =
    | AddItem of string
    | RemoveItem of int

  type Actions2 =
    | AddItem of string
    | RemoveItem of int

  type Actions3 =
    | AddItem of string
    | RemoveItem of int

  let add x y = x + y
  let mul x y = x * y

  let three = add 3 4 |> mul 3 |> add 2 |> add 3


module Test2536 =
  type Actions =
    | AddItem of string
    | RemoveItem of int

  type Actions2 =
    | AddItem of string
    | RemoveItem of int

  type Actions3 =
    | AddItem of string
    | RemoveItem of int

  let add x y = x + y
  let mul x y = x * y

  let three = add 3 4 |> mul 3 |> add 2 |> add 3


module Test2537 =
  type Actions =
    | AddItem of string
    | RemoveItem of int

  type Actions2 =
    | AddItem of string
    | RemoveItem of int

  type Actions3 =
    | AddItem of string
    | RemoveItem of int

  let add x y = x + y
  let mul x y = x * y

  let three = add 3 4 |> mul 3 |> add 2 |> add 3


module Test2538 =
  type Actions =
    | AddItem of string
    | RemoveItem of int

  type Actions2 =
    | AddItem of string
    | RemoveItem of int

  type Actions3 =
    | AddItem of string
    | RemoveItem of int

  let add x y = x + y
  let mul x y = x * y

  let three = add 3 4 |> mul 3 |> add 2 |> add 3


module Test2539 =
  type Actions =
    | AddItem of string
    | RemoveItem of int

  type Actions2 =
    | AddItem of string
    | RemoveItem of int

  type Actions3 =
    | AddItem of string
    | RemoveItem of int

  let add x y = x + y
  let mul x y = x * y

  let three = add 3 4 |> mul 3 |> add 2 |> add 3


module Test2540 =
  type Actions =
    | AddItem of string
    | RemoveItem of int

  type Actions2 =
    | AddItem of string
    | RemoveItem of int

  type Actions3 =
    | AddItem of string
    | RemoveItem of int

  let add x y = x + y
  let mul x y = x * y

  let three = add 3 4 |> mul 3 |> add 2 |> add 3


module Test2541 =
  type Actions =
    | AddItem of string
    | RemoveItem of int

  type Actions2 =
    | AddItem of string
    | RemoveItem of int

  type Actions3 =
    | AddItem of string
    | RemoveItem of int

  let add x y = x + y
  let mul x y = x * y

  let three = add 3 4 |> mul 3 |> add 2 |> add 3


module Test2542 =
  type Actions =
    | AddItem of string
    | RemoveItem of int

  type Actions2 =
    | AddItem of string
    | RemoveItem of int

  type Actions3 =
    | AddItem of string
    | RemoveItem of int

  let add x y = x + y
  let mul x y = x * y

  let three = add 3 4 |> mul 3 |> add 2 |> add 3


module Test2543 =
  type Actions =
    | AddItem of string
    | RemoveItem of int

  type Actions2 =
    | AddItem of string
    | RemoveItem of int

  type Actions3 =
    | AddItem of string
    | RemoveItem of int

  let add x y = x + y
  let mul x y = x * y

  let three = add 3 4 |> mul 3 |> add 2 |> add 3


module Test2544 =
  type Actions =
    | AddItem of string
    | RemoveItem of int

  type Actions2 =
    | AddItem of string
    | RemoveItem of int

  type Actions3 =
    | AddItem of string
    | RemoveItem of int

  let add x y = x + y
  let mul x y = x * y

  let three = add 3 4 |> mul 3 |> add 2 |> add 3


module Test2545 =
  type Actions =
    | AddItem of string
    | RemoveItem of int

  type Actions2 =
    | AddItem of string
    | RemoveItem of int

  type Actions3 =
    | AddItem of string
    | RemoveItem of int

  let add x y = x + y
  let mul x y = x * y

  let three = add 3 4 |> mul 3 |> add 2 |> add 3


module Test2546 =
  type Actions =
    | AddItem of string
    | RemoveItem of int

  type Actions2 =
    | AddItem of string
    | RemoveItem of int

  type Actions3 =
    | AddItem of string
    | RemoveItem of int

  let add x y = x + y
  let mul x y = x * y

  let three = add 3 4 |> mul 3 |> add 2 |> add 3


module Test2547 =
  type Actions =
    | AddItem of string
    | RemoveItem of int

  type Actions2 =
    | AddItem of string
    | RemoveItem of int

  type Actions3 =
    | AddItem of string
    | RemoveItem of int

  let add x y = x + y
  let mul x y = x * y

  let three = add 3 4 |> mul 3 |> add 2 |> add 3


module Test2548 =
  type Actions =
    | AddItem of string
    | RemoveItem of int

  type Actions2 =
    | AddItem of string
    | RemoveItem of int

  type Actions3 =
    | AddItem of string
    | RemoveItem of int

  let add x y = x + y
  let mul x y = x * y

  let three = add 3 4 |> mul 3 |> add 2 |> add 3


module Test2549 =
  type Actions =
    | AddItem of string
    | RemoveItem of int

  type Actions2 =
    | AddItem of string
    | RemoveItem of int

  type Actions3 =
    | AddItem of string
    | RemoveItem of int

  let add x y = x + y
  let mul x y = x * y

  let three = add 3 4 |> mul 3 |> add 2 |> add 3


module Test2550 =
  type Actions =
    | AddItem of string
    | RemoveItem of int

  type Actions2 =
    | AddItem of string
    | RemoveItem of int

  type Actions3 =
    | AddItem of string
    | RemoveItem of int

  let add x y = x + y
  let mul x y = x * y

  let three = add 3 4 |> mul 3 |> add 2 |> add 3

