module Test54
module Test541 =
  type Actions =
    | AddItem of string
    | RemoveItem of int

  type Actions2 =
    | AddItem of string
    | RemoveItem of int

  type Actions3 =
    | AddItem of string
    | RemoveItem of int

  let add x y = x + y
  let mul x y = x * y

  let three = add 3 4 |> mul 3 |> add 2 |> add 3


module Test542 =
  type Actions =
    | AddItem of string
    | RemoveItem of int

  type Actions2 =
    | AddItem of string
    | RemoveItem of int

  type Actions3 =
    | AddItem of string
    | RemoveItem of int

  let add x y = x + y
  let mul x y = x * y

  let three = add 3 4 |> mul 3 |> add 2 |> add 3


module Test543 =
  type Actions =
    | AddItem of string
    | RemoveItem of int

  type Actions2 =
    | AddItem of string
    | RemoveItem of int

  type Actions3 =
    | AddItem of string
    | RemoveItem of int

  let add x y = x + y
  let mul x y = x * y

  let three = add 3 4 |> mul 3 |> add 2 |> add 3


module Test544 =
  type Actions =
    | AddItem of string
    | RemoveItem of int

  type Actions2 =
    | AddItem of string
    | RemoveItem of int

  type Actions3 =
    | AddItem of string
    | RemoveItem of int

  let add x y = x + y
  let mul x y = x * y

  let three = add 3 4 |> mul 3 |> add 2 |> add 3


module Test545 =
  type Actions =
    | AddItem of string
    | RemoveItem of int

  type Actions2 =
    | AddItem of string
    | RemoveItem of int

  type Actions3 =
    | AddItem of string
    | RemoveItem of int

  let add x y = x + y
  let mul x y = x * y

  let three = add 3 4 |> mul 3 |> add 2 |> add 3


module Test546 =
  type Actions =
    | AddItem of string
    | RemoveItem of int

  type Actions2 =
    | AddItem of string
    | RemoveItem of int

  type Actions3 =
    | AddItem of string
    | RemoveItem of int

  let add x y = x + y
  let mul x y = x * y

  let three = add 3 4 |> mul 3 |> add 2 |> add 3


module Test547 =
  type Actions =
    | AddItem of string
    | RemoveItem of int

  type Actions2 =
    | AddItem of string
    | RemoveItem of int

  type Actions3 =
    | AddItem of string
    | RemoveItem of int

  let add x y = x + y
  let mul x y = x * y

  let three = add 3 4 |> mul 3 |> add 2 |> add 3


module Test548 =
  type Actions =
    | AddItem of string
    | RemoveItem of int

  type Actions2 =
    | AddItem of string
    | RemoveItem of int

  type Actions3 =
    | AddItem of string
    | RemoveItem of int

  let add x y = x + y
  let mul x y = x * y

  let three = add 3 4 |> mul 3 |> add 2 |> add 3


module Test549 =
  type Actions =
    | AddItem of string
    | RemoveItem of int

  type Actions2 =
    | AddItem of string
    | RemoveItem of int

  type Actions3 =
    | AddItem of string
    | RemoveItem of int

  let add x y = x + y
  let mul x y = x * y

  let three = add 3 4 |> mul 3 |> add 2 |> add 3


module Test5410 =
  type Actions =
    | AddItem of string
    | RemoveItem of int

  type Actions2 =
    | AddItem of string
    | RemoveItem of int

  type Actions3 =
    | AddItem of string
    | RemoveItem of int

  let add x y = x + y
  let mul x y = x * y

  let three = add 3 4 |> mul 3 |> add 2 |> add 3


module Test5411 =
  type Actions =
    | AddItem of string
    | RemoveItem of int

  type Actions2 =
    | AddItem of string
    | RemoveItem of int

  type Actions3 =
    | AddItem of string
    | RemoveItem of int

  let add x y = x + y
  let mul x y = x * y

  let three = add 3 4 |> mul 3 |> add 2 |> add 3


module Test5412 =
  type Actions =
    | AddItem of string
    | RemoveItem of int

  type Actions2 =
    | AddItem of string
    | RemoveItem of int

  type Actions3 =
    | AddItem of string
    | RemoveItem of int

  let add x y = x + y
  let mul x y = x * y

  let three = add 3 4 |> mul 3 |> add 2 |> add 3


module Test5413 =
  type Actions =
    | AddItem of string
    | RemoveItem of int

  type Actions2 =
    | AddItem of string
    | RemoveItem of int

  type Actions3 =
    | AddItem of string
    | RemoveItem of int

  let add x y = x + y
  let mul x y = x * y

  let three = add 3 4 |> mul 3 |> add 2 |> add 3


module Test5414 =
  type Actions =
    | AddItem of string
    | RemoveItem of int

  type Actions2 =
    | AddItem of string
    | RemoveItem of int

  type Actions3 =
    | AddItem of string
    | RemoveItem of int

  let add x y = x + y
  let mul x y = x * y

  let three = add 3 4 |> mul 3 |> add 2 |> add 3


module Test5415 =
  type Actions =
    | AddItem of string
    | RemoveItem of int

  type Actions2 =
    | AddItem of string
    | RemoveItem of int

  type Actions3 =
    | AddItem of string
    | RemoveItem of int

  let add x y = x + y
  let mul x y = x * y

  let three = add 3 4 |> mul 3 |> add 2 |> add 3


module Test5416 =
  type Actions =
    | AddItem of string
    | RemoveItem of int

  type Actions2 =
    | AddItem of string
    | RemoveItem of int

  type Actions3 =
    | AddItem of string
    | RemoveItem of int

  let add x y = x + y
  let mul x y = x * y

  let three = add 3 4 |> mul 3 |> add 2 |> add 3


module Test5417 =
  type Actions =
    | AddItem of string
    | RemoveItem of int

  type Actions2 =
    | AddItem of string
    | RemoveItem of int

  type Actions3 =
    | AddItem of string
    | RemoveItem of int

  let add x y = x + y
  let mul x y = x * y

  let three = add 3 4 |> mul 3 |> add 2 |> add 3


module Test5418 =
  type Actions =
    | AddItem of string
    | RemoveItem of int

  type Actions2 =
    | AddItem of string
    | RemoveItem of int

  type Actions3 =
    | AddItem of string
    | RemoveItem of int

  let add x y = x + y
  let mul x y = x * y

  let three = add 3 4 |> mul 3 |> add 2 |> add 3


module Test5419 =
  type Actions =
    | AddItem of string
    | RemoveItem of int

  type Actions2 =
    | AddItem of string
    | RemoveItem of int

  type Actions3 =
    | AddItem of string
    | RemoveItem of int

  let add x y = x + y
  let mul x y = x * y

  let three = add 3 4 |> mul 3 |> add 2 |> add 3


module Test5420 =
  type Actions =
    | AddItem of string
    | RemoveItem of int

  type Actions2 =
    | AddItem of string
    | RemoveItem of int

  type Actions3 =
    | AddItem of string
    | RemoveItem of int

  let add x y = x + y
  let mul x y = x * y

  let three = add 3 4 |> mul 3 |> add 2 |> add 3


module Test5421 =
  type Actions =
    | AddItem of string
    | RemoveItem of int

  type Actions2 =
    | AddItem of string
    | RemoveItem of int

  type Actions3 =
    | AddItem of string
    | RemoveItem of int

  let add x y = x + y
  let mul x y = x * y

  let three = add 3 4 |> mul 3 |> add 2 |> add 3


module Test5422 =
  type Actions =
    | AddItem of string
    | RemoveItem of int

  type Actions2 =
    | AddItem of string
    | RemoveItem of int

  type Actions3 =
    | AddItem of string
    | RemoveItem of int

  let add x y = x + y
  let mul x y = x * y

  let three = add 3 4 |> mul 3 |> add 2 |> add 3


module Test5423 =
  type Actions =
    | AddItem of string
    | RemoveItem of int

  type Actions2 =
    | AddItem of string
    | RemoveItem of int

  type Actions3 =
    | AddItem of string
    | RemoveItem of int

  let add x y = x + y
  let mul x y = x * y

  let three = add 3 4 |> mul 3 |> add 2 |> add 3


module Test5424 =
  type Actions =
    | AddItem of string
    | RemoveItem of int

  type Actions2 =
    | AddItem of string
    | RemoveItem of int

  type Actions3 =
    | AddItem of string
    | RemoveItem of int

  let add x y = x + y
  let mul x y = x * y

  let three = add 3 4 |> mul 3 |> add 2 |> add 3


module Test5425 =
  type Actions =
    | AddItem of string
    | RemoveItem of int

  type Actions2 =
    | AddItem of string
    | RemoveItem of int

  type Actions3 =
    | AddItem of string
    | RemoveItem of int

  let add x y = x + y
  let mul x y = x * y

  let three = add 3 4 |> mul 3 |> add 2 |> add 3


module Test5426 =
  type Actions =
    | AddItem of string
    | RemoveItem of int

  type Actions2 =
    | AddItem of string
    | RemoveItem of int

  type Actions3 =
    | AddItem of string
    | RemoveItem of int

  let add x y = x + y
  let mul x y = x * y

  let three = add 3 4 |> mul 3 |> add 2 |> add 3


module Test5427 =
  type Actions =
    | AddItem of string
    | RemoveItem of int

  type Actions2 =
    | AddItem of string
    | RemoveItem of int

  type Actions3 =
    | AddItem of string
    | RemoveItem of int

  let add x y = x + y
  let mul x y = x * y

  let three = add 3 4 |> mul 3 |> add 2 |> add 3


module Test5428 =
  type Actions =
    | AddItem of string
    | RemoveItem of int

  type Actions2 =
    | AddItem of string
    | RemoveItem of int

  type Actions3 =
    | AddItem of string
    | RemoveItem of int

  let add x y = x + y
  let mul x y = x * y

  let three = add 3 4 |> mul 3 |> add 2 |> add 3


module Test5429 =
  type Actions =
    | AddItem of string
    | RemoveItem of int

  type Actions2 =
    | AddItem of string
    | RemoveItem of int

  type Actions3 =
    | AddItem of string
    | RemoveItem of int

  let add x y = x + y
  let mul x y = x * y

  let three = add 3 4 |> mul 3 |> add 2 |> add 3


module Test5430 =
  type Actions =
    | AddItem of string
    | RemoveItem of int

  type Actions2 =
    | AddItem of string
    | RemoveItem of int

  type Actions3 =
    | AddItem of string
    | RemoveItem of int

  let add x y = x + y
  let mul x y = x * y

  let three = add 3 4 |> mul 3 |> add 2 |> add 3


module Test5431 =
  type Actions =
    | AddItem of string
    | RemoveItem of int

  type Actions2 =
    | AddItem of string
    | RemoveItem of int

  type Actions3 =
    | AddItem of string
    | RemoveItem of int

  let add x y = x + y
  let mul x y = x * y

  let three = add 3 4 |> mul 3 |> add 2 |> add 3


module Test5432 =
  type Actions =
    | AddItem of string
    | RemoveItem of int

  type Actions2 =
    | AddItem of string
    | RemoveItem of int

  type Actions3 =
    | AddItem of string
    | RemoveItem of int

  let add x y = x + y
  let mul x y = x * y

  let three = add 3 4 |> mul 3 |> add 2 |> add 3


module Test5433 =
  type Actions =
    | AddItem of string
    | RemoveItem of int

  type Actions2 =
    | AddItem of string
    | RemoveItem of int

  type Actions3 =
    | AddItem of string
    | RemoveItem of int

  let add x y = x + y
  let mul x y = x * y

  let three = add 3 4 |> mul 3 |> add 2 |> add 3


module Test5434 =
  type Actions =
    | AddItem of string
    | RemoveItem of int

  type Actions2 =
    | AddItem of string
    | RemoveItem of int

  type Actions3 =
    | AddItem of string
    | RemoveItem of int

  let add x y = x + y
  let mul x y = x * y

  let three = add 3 4 |> mul 3 |> add 2 |> add 3


module Test5435 =
  type Actions =
    | AddItem of string
    | RemoveItem of int

  type Actions2 =
    | AddItem of string
    | RemoveItem of int

  type Actions3 =
    | AddItem of string
    | RemoveItem of int

  let add x y = x + y
  let mul x y = x * y

  let three = add 3 4 |> mul 3 |> add 2 |> add 3


module Test5436 =
  type Actions =
    | AddItem of string
    | RemoveItem of int

  type Actions2 =
    | AddItem of string
    | RemoveItem of int

  type Actions3 =
    | AddItem of string
    | RemoveItem of int

  let add x y = x + y
  let mul x y = x * y

  let three = add 3 4 |> mul 3 |> add 2 |> add 3


module Test5437 =
  type Actions =
    | AddItem of string
    | RemoveItem of int

  type Actions2 =
    | AddItem of string
    | RemoveItem of int

  type Actions3 =
    | AddItem of string
    | RemoveItem of int

  let add x y = x + y
  let mul x y = x * y

  let three = add 3 4 |> mul 3 |> add 2 |> add 3


module Test5438 =
  type Actions =
    | AddItem of string
    | RemoveItem of int

  type Actions2 =
    | AddItem of string
    | RemoveItem of int

  type Actions3 =
    | AddItem of string
    | RemoveItem of int

  let add x y = x + y
  let mul x y = x * y

  let three = add 3 4 |> mul 3 |> add 2 |> add 3


module Test5439 =
  type Actions =
    | AddItem of string
    | RemoveItem of int

  type Actions2 =
    | AddItem of string
    | RemoveItem of int

  type Actions3 =
    | AddItem of string
    | RemoveItem of int

  let add x y = x + y
  let mul x y = x * y

  let three = add 3 4 |> mul 3 |> add 2 |> add 3


module Test5440 =
  type Actions =
    | AddItem of string
    | RemoveItem of int

  type Actions2 =
    | AddItem of string
    | RemoveItem of int

  type Actions3 =
    | AddItem of string
    | RemoveItem of int

  let add x y = x + y
  let mul x y = x * y

  let three = add 3 4 |> mul 3 |> add 2 |> add 3


module Test5441 =
  type Actions =
    | AddItem of string
    | RemoveItem of int

  type Actions2 =
    | AddItem of string
    | RemoveItem of int

  type Actions3 =
    | AddItem of string
    | RemoveItem of int

  let add x y = x + y
  let mul x y = x * y

  let three = add 3 4 |> mul 3 |> add 2 |> add 3


module Test5442 =
  type Actions =
    | AddItem of string
    | RemoveItem of int

  type Actions2 =
    | AddItem of string
    | RemoveItem of int

  type Actions3 =
    | AddItem of string
    | RemoveItem of int

  let add x y = x + y
  let mul x y = x * y

  let three = add 3 4 |> mul 3 |> add 2 |> add 3


module Test5443 =
  type Actions =
    | AddItem of string
    | RemoveItem of int

  type Actions2 =
    | AddItem of string
    | RemoveItem of int

  type Actions3 =
    | AddItem of string
    | RemoveItem of int

  let add x y = x + y
  let mul x y = x * y

  let three = add 3 4 |> mul 3 |> add 2 |> add 3


module Test5444 =
  type Actions =
    | AddItem of string
    | RemoveItem of int

  type Actions2 =
    | AddItem of string
    | RemoveItem of int

  type Actions3 =
    | AddItem of string
    | RemoveItem of int

  let add x y = x + y
  let mul x y = x * y

  let three = add 3 4 |> mul 3 |> add 2 |> add 3


module Test5445 =
  type Actions =
    | AddItem of string
    | RemoveItem of int

  type Actions2 =
    | AddItem of string
    | RemoveItem of int

  type Actions3 =
    | AddItem of string
    | RemoveItem of int

  let add x y = x + y
  let mul x y = x * y

  let three = add 3 4 |> mul 3 |> add 2 |> add 3


module Test5446 =
  type Actions =
    | AddItem of string
    | RemoveItem of int

  type Actions2 =
    | AddItem of string
    | RemoveItem of int

  type Actions3 =
    | AddItem of string
    | RemoveItem of int

  let add x y = x + y
  let mul x y = x * y

  let three = add 3 4 |> mul 3 |> add 2 |> add 3


module Test5447 =
  type Actions =
    | AddItem of string
    | RemoveItem of int

  type Actions2 =
    | AddItem of string
    | RemoveItem of int

  type Actions3 =
    | AddItem of string
    | RemoveItem of int

  let add x y = x + y
  let mul x y = x * y

  let three = add 3 4 |> mul 3 |> add 2 |> add 3


module Test5448 =
  type Actions =
    | AddItem of string
    | RemoveItem of int

  type Actions2 =
    | AddItem of string
    | RemoveItem of int

  type Actions3 =
    | AddItem of string
    | RemoveItem of int

  let add x y = x + y
  let mul x y = x * y

  let three = add 3 4 |> mul 3 |> add 2 |> add 3


module Test5449 =
  type Actions =
    | AddItem of string
    | RemoveItem of int

  type Actions2 =
    | AddItem of string
    | RemoveItem of int

  type Actions3 =
    | AddItem of string
    | RemoveItem of int

  let add x y = x + y
  let mul x y = x * y

  let three = add 3 4 |> mul 3 |> add 2 |> add 3


module Test5450 =
  type Actions =
    | AddItem of string
    | RemoveItem of int

  type Actions2 =
    | AddItem of string
    | RemoveItem of int

  type Actions3 =
    | AddItem of string
    | RemoveItem of int

  let add x y = x + y
  let mul x y = x * y

  let three = add 3 4 |> mul 3 |> add 2 |> add 3

